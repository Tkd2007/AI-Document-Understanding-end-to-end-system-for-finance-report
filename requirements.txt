# --- Xử lý ảnh / PDF ---
pillow
pdf2image          # cần Poppler cài sẵn ở máy, xem POPPLER_PATH trong README
numpy

# --- Layout detection ---
doclayout-yolo     # cung cấp `from doclayout_yolo import YOLOv10`
huggingface-hub    # tải checkpoint DocLayout-YOLO qua hf_hub_download()

# --- Nhánh OCR ---
easyocr

# --- Nhánh VLM ---
openai             # client trỏ sang OpenRouter, không gọi thẳng OpenAI

# --- API layer ---
fastapi
uvicorn
python-multipart   # bắt buộc để FastAPI nhận được UploadFile

# --- Config ---
python-dotenv

# Ghi chú: torch được cài kèm theo easyocr và doclayout-yolo nên không
# khai báo ở đây. Nếu muốn chạy GPU, cài torch riêng theo đúng bản CUDA
# của máy TRƯỚC khi chạy lệnh này (xem https://pytorch.org/get-started).
